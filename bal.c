#include<stdio.h>
#include<string.h>

main()
{
    int cnt=0;
    char a[100],b[10],*ret;
    gets(a);
    gets(b);
    while(r
    {
        ret=strstr(a,b);
        a[ret-a]='#';
        cnt++;
    }
    printf("%d",cnt);

}
