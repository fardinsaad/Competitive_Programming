#include<bits/stdc++.h>
using namespace std;

int main()
{
    string a[5],b[5],c[5],x,y;
    int n;
    cin >> x;
    cin >> y;
    cin >> n;
    for(int i = 0; i < n; i++)
    {
        cin >> a[i] >> b[i] >> c[i];
    }
    cout << b[1] << endl <<c[0];
}
