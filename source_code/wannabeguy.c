#include<stdio.h>
#include<string.h>

int main()
{
    int i,j,k=0,n,x,y,flag=1,m;
    char a[105],b[105];
    scanf("%d",&x);
    getchar();
    //for(i=0;i<(2*x)-1;i++)scanf("%c",&a[i]);
    gets(a);
    scanf("%d",&y);
    getchar();
   //for(m=0;m<(2*y)-1;m++)scanf("%c",&b[m]);
    gets(b);
    puts(a);
    puts(b);
}
